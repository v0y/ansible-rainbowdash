Ansible for Rainbow Dash VPS
============================

Install roles:

```sh
ansible-galaxy install -r install_roles.yml -f
```

Run roles:

```sh
ansible-playbook setup_rainbowdash.yml -i inventory
```
